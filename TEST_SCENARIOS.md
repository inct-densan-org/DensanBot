# 電算部Bot テストシナリオ

このドキュメントは、Botの全機能が正常に動作することを確認するためのテスト手順書です。

## 1. 初期設定と起動

- [ ] **設定ファイルの確認**: `.env` ファイルに正しいTOKENとチャンネルIDが設定されているか確認する。
- [ ] **テンプレートファイルの確認**: ルートディレクトリにExcelテンプレートファイルが存在するか確認する。
- [ ] **Botの起動**: `start.bat` (Windows) または `start.sh` (Linux) を実行し、コンソールにエラーが出ずに「ログインしました」と表示されるか確認する。

## 2. 基本設定 (`/config`)

- [ ] **設定表示**: `/config show` を実行し、現在の設定値が表示されるか確認する。
- [ ] **顧問名の変更**: `/config set advisor 名前:テスト顧問` を実行し、反映されるか確認する。
- [ ] **代表学生名の変更**: `/config set student_rep 名前:テスト学生` を実行し、反映されるか確認する。
- [ ] **グループの追加**: `/config group add name:新規グループ` を実行する。
- [ ] **場所の追加**: `/config location add name:新規場所` を実行する。
- [ ] **再起動**: `/admin restart` を実行し、Botが再起動することを確認する。再起動後、`/plan add` などのコマンドで追加したグループ・場所が選択肢に出るか確認する。
- [ ] **グループ・場所の削除**: `/config group remove` と `/config location remove` で追加した項目を削除し、再度 `/admin restart` して反映を確認する。

## 3. スケジュール管理 (`/schedule`)

- [ ] **定期活動の登録**: `/schedule regular add` で、特定の曜日の定期活動を登録する。
- [ ] **定期活動の一覧**: `/schedule regular list` を実行し、登録した活動が表示されるか確認する。ページングと削除機能もテストする。
- [ ] **休止期間の登録**: `/schedule off-period add` で、テスト期間などの休止期間を登録する。
- [ ] **休止期間の一覧**: `/schedule off-period list` を実行し、登録した期間が表示されるか確認する。削除機能もテストする。
- [ ] **シート生成**: `/schedule generate_sheet` を実行する。
    - [ ] 生成されたExcelファイルを開き、定期活動が反映されているか確認する。
    - [ ] 休止期間中の日付には活動が入っていないことを確認する。

## 4. 活動計画 (`/plan`)

- [ ] **計画の追加 (通常)**: `/plan add` を実行し、既存のグループ・場所を選択して計画を登録する。
- [ ] **計画の追加 (その他)**: `/plan add` でグループまたは場所に「その他」を選択し、モーダルで手動入力して登録できるか確認する。
- [ ] **計画の一覧**: `/plan list` を実行する。
    - [ ] 登録した計画が表示されるか確認する。
    - [ ] **絞り込み**: 引数 `group` を指定して、特定のグループのみ表示されるか確認する。
    - [ ] **ページング**: 項目数が多い場合、ページ送りが機能するか確認する。
    - [ ] **編集**: ドロップダウンから計画を選択し、「編集」ボタンで内容を変更できるか確認する。
    - [ ] **削除**: ドロップダウンから計画を選択し、「削除」ボタンで削除できるか確認する。

## 5. 活動報告 (`/report`)

- [ ] **コマンドからの報告**: `/report open` を実行する。
    - [ ] グループと場所を選択し、モーダルが開くか確認する。
    - [ ] 今日の計画がある場合、時間が自動入力されているか確認する。
    - [ ] 報告完了後、通知チャンネルにEmbedメッセージが送信されるか確認する。
- [ ] **リマインダーからの報告**:
    - [ ] `/schedule send_reminder group:テストグループ` を実行する。
    - [ ] 送信されたメッセージの「活動報告を行う」ボタンを押す。
    - [ ] グループ選択 -> 場所選択 -> モーダル表示 のフローが正常に動作するか確認する。
- [ ] **「その他」の入力**: 報告時にグループや場所で「その他」を選び、自由入力ができるか確認する。

## 6. 履歴管理 (`/history`)

- [ ] **報告履歴の閲覧**: `/history report` を実行し、過去の報告が表示されるか確認する。
    - [ ] ドロップダウンから選択して詳細を表示し、「削除」ボタンで報告を取り消せるか確認する。
- [ ] **未報告計画の確認**: `/history unreported` を実行する。
    - [ ] 過去の日付で、まだ報告していない計画が表示されるか確認する。
    - [ ] ドロップダウンから選択し、「活動なしとして報告」ボタンを押して処理できるか確認する。

## 7. カスタムリマインダー (`/remind`)

- [ ] **リマインダー登録**: `/remind add` で新しいタスクを登録する。
- [ ] **リマインダー一覧**: `/remind list` を実行する。
    - [ ] 登録したタスクが表示されるか確認する。
    - [ ] ドロップダウンから選択し、「編集」で内容を変更できるか確認する。
    - [ ] ドロップダウンから選択し、「削除」でタスクを削除できるか確認する。

## 8. Excel出力 (`/excel`)

- [ ] **Excel出力**: `/excel export` を実行する。
    - [ ] zip形式でファイルが送信されるか確認する。
    - [ ] `as_zip:False` オプションを指定して、生ファイルが送信されるか確認する。
    - [ ] 出力されたファイルを開き、計画・報告内容が正しく記録されているか確認する。

## 9. 管理者機能 (`/admin`)

- [ ] **リロード**: `/admin reload` を実行し、エラーなく完了するか確認する。
- [ ] **再起動**: `/admin restart` を実行し、Botプロセスが再起動するか確認する。
